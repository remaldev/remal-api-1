meta {
  name: login
  type: http
  seq: 3
}

post {
  url: {{host}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@remal.dev",
    "password": "TestPsswd+1"
  }
}

vars:post-response {
  access_token: res.body?.data?.access_token
}
